plugins {
    id 'java'
    id 'application'
}

repositories {
    mavenCentral()
    maven {
        url 'http://eaglejs-mac-mini.local:8091/'
        allowInsecureProtocol = true
    }
}

dependencies {
    implementation 'io.dropwizard:dropwizard-core:2.0.24'
    implementation 'io.dropwizard:dropwizard-client:2.0.24'
    implementation 'javax.websocket:javax.websocket-api:1.1'
    implementation 'org.eclipse.jetty.websocket:javax-websocket-server-impl:9.4.43.v20210629'
    implementation 'org.mongodb:mongodb-driver-sync:4.3.4'
    implementation 'org.mongodb:mongodb-driver-core:4.3.4'
    implementation 'org.mongodb:mongodb-driver-legacy:4.3.4'
    implementation 'org.glassfish.jersey.core:jersey-client:2.32'
    implementation 'org.glassfish.jersey.inject:jersey-hk2:2.32'
    implementation 'org.glassfish.jersey.media:jersey-media-json-jackson:2.32'
    implementation 'io.swagger.core.v3:swagger-jaxrs2:2.1.11'
    implementation 'io.swagger.core.v3:swagger-annotations:2.1.11'

    testImplementation 'io.dropwizard:dropwizard-testing:2.0.24'
    testImplementation 'junit:junit:4.13.2'
    testImplementation 'org.mockito:mockito-core:4.0.0'
}

test {
    useJUnit()
}

application {
    mainClass = 'io.eaglejs.ffxi.Main'
}

jar {
    duplicatesStrategy = DuplicatesStrategy.EXCLUDE
    from {
        configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
    }
    manifest {
        attributes 'Main-Class': 'io.eaglejs.ffxi.Main'
    }
}

// Task to create a placeholder UI package for demonstration
task createPlaceholderUI {
    description = 'Create placeholder UI files if ffxi-stats package is not available'
    def assetsDir = file("${project.buildDir}/resources/main/assets")
    
    doLast {
        assetsDir.mkdirs()
        def indexHtml = new File(assetsDir, "index.html")
        if (!indexHtml.exists()) {
            indexHtml.text = """<!DOCTYPE html>
<html>
<head>
    <title>FFXI Player Stats</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 40px; }
        h1 { color: #333; }
        .status { color: #28a745; }
    </style>
</head>
<body>
    <h1>FFXI Player Service</h1>
    <p class="status">Service is running</p>
    <p>To install the ffxi-stats UI package, download ffxi-stats@1.0.0 from:</p>
    <code>http://eaglejs-mac-mini.local:8091/repository/npm-hosted/ffxi-stats/</code>
    <h2>Available Endpoints:</h2>
    <ul>
        <li><a href="/health">/health</a> - Health check endpoint</li>
        <li><a href="/openapi.json">/openapi.json</a> - API documentation</li>
        <li>ws://localhost/ws/players - WebSocket for player stats</li>
    </ul>
</body>
</html>"""
            logger.lifecycle("Created placeholder UI at ${indexHtml}")
        }
    }
}

task copyWebApp(type: Copy) {
    from 'src/main/webapp'
    into "$buildDir/resources/main/webapp"
}

processResources.dependsOn copyWebApp, createPlaceholderUI